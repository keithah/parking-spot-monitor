from __future__ import annotations

from parking_spot_monitor.vehicle_history_archive import (
    VehicleHistoryArchive,
    attach_occupied_images,
    close_session,
    cutoff_older_than_days,
    estimate_profile_history,
    estimate_session_history,
    export_archive,
    health_snapshot,
    list_closed_sessions,
    load_active_sessions,
    mark_wrong_match,
    match_or_create_profile,
    merge_profiles,
    profile_summary,
    prune_closed_sessions,
    rename_profile,
    start_session,
)
from parking_spot_monitor.vehicle_history_models import (
    CORRECTION_ACTION_MERGE_PROFILES,
    CORRECTION_ACTION_PROFILE_SUMMARY_REQUESTED,
    CORRECTION_ACTION_RENAME_PROFILE,
    CORRECTION_ACTION_WRONG_MATCH,
    ArchiveSchemaError,
    ArchiveWriteError,
    CorrectionReplayState,
    ProfileAssignment,
    ProfileCorrectionEvent,
    SessionRecord,
    StoredVehicleProfile,
    VehicleHistoryExportResult,
    VehicleHistoryPruneResult,
    _archive_directory_stats,
    _archive_files_for_export,
    _archive_local_path,
    _archive_member_name,
    _bounded_string,
    _coerce_cutoff_datetime,
    _correction_id,
    _descriptor_from_json,
    _descriptor_to_json,
    _duration_seconds,
    _event_payload,
    _event_time,
    _finite_float,
    _image_directory_stats,
    _maintenance_log_fields,
    _missing_occupied_image_reference_count,
    _oldest_retained_session_started_at,
    _optional_bounded_string,
    _optional_dict,
    _optional_event_snapshot,
    _optional_finite_float,
    _optional_non_negative_int,
    _optional_profile_id,
    _optional_string,
    _parse_timestamp,
    _profile_quarantine_count,
    _record_archive_image_paths,
    _record_closed_before,
    _referenced_archive_paths,
    _safe_error_message,
    _safe_file_size,
    _safe_maintenance_metadata,
    _safe_resolve,
    _session_id,
    _session_with_profile,
    _slug,
    _string,
    _utc_now,
    _validate_close_event,
    _validate_json_safe,
    _validate_start_event,
)
from parking_spot_monitor.vehicle_profiles import match_vehicle_profile
